<?php

declare(strict_types=1);

namespace PhpQueues\Transport;

/**
 * The message is defined on the transport side and may contain transport-specific flags,
 * headers (if supported) and content.
 */
interface Message
{
}
